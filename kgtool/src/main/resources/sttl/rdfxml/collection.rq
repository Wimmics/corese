template st:collection(?x, ?p, ?y) {
format {
"<%s rdf:parseType='Collection'>%s</%s>"
   
    st:turtle(?p, true)
    box { st:call-template(st:list, ?y) }
    st:turtle(?p, true)
    
   }
}
where {
  
}
